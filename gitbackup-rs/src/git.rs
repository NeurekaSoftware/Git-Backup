//! Git ← `Services/Git/`.
//!
//! `git`/`git-lfs` CLI wrapper: bare `--mirror` clones, incremental fetch, self-heal, transport
//! allowlist, origin-scoped credential env injection, process-tree kill. Ported in phase P4.
